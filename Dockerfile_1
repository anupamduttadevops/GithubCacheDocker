# Base image for the final image
FROM alpine:latest

WORKDIR /root/

# Copy the pre-built binary from the workflow
COPY myapp .

# Expose the application port
EXPOSE 8080

# Command to run the application
CMD ["./myapp"]
